<!doctype html>
<html lang="en">
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>
    <title>twitvia - <%= @title || "Powered by TwitterAuth" %></title>
	<meta name="description" content="" />
	<meta name="keywords" content="" />
	
    <%= stylesheet_link_tag 'reset', 'typography', 'base' %>
    <%= javascript_include_tag :defaults %>
  </head>
<body>
	<div class="container">
		<%= render "layouts/brand" %>
		
		<% if flash[:error] %>
          <div class='flash error'><%= flash[:error] %></div>
        <% end %><% if flash[:notice] %>
          <div class='flash notice'><%= flash[:notice] %></div>
        <% end %>

		<div class="content">
			<div class="body">
				<section>			
					<header>
					  <h2>The Question</h2>
					</header>
					
					<p>This is where the question will go, it will look something like this</p>
					<p>Actor Actor Actor</p>
					<p>This question is closed, the next question will be asked in 15 minutes (countdown)</p>
				</section>
			    
			    <%= yield %>

			</div>
			<%= render "layouts/sidebar" %>
		</div>
		<%= render "layouts/footer" %>
	</div>
	<%#= render "layouts/analytics" %>
</body>
</html>
